query searchFile(
  $expression: String!
  $repositoryName: String!
  $owner: String!
) {
  repository(name: $repositoryName, owner: $owner) {
    id
    object(expression: $expression) {
      ... on Tree {
        entries {
          type
          name
        }
      }
    }
  }
}
